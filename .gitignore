node_modules
dist
public
.env
.env.local
.DS_Store
*.log
node_modules/.tmp

# Appify build artifacts — regenerable via `npm run desktop:build`
desktop/
assets/icons/

# Local-only acceptance screenshots
tournament-acceptance-*.png

# Local tournament data, campaigns, and plans belong to each user's workspace.
tournaments/
campaigns/
plans/
